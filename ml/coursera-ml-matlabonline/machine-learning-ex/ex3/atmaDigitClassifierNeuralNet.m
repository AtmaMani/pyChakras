% load subset of MNIST datasetload('ex3data1.mat');  % loads X and y into mem% display a random set of imagescm = size(X,1);% randomly select 100 data points to displaysel = randperm(size(X,1));sel = sel(1:100);displayData(X(sel,:));% load saved neural net weights into memoryload('ex3weights.mat'); %loads Theta1 (25x401) and Theta2(26x10) into mem% predict on training setpred = predict(Theta1, Theta2, X);fprintf('\nTraining set accuracy: %f\n', mean(double(pred==y))*100);